package com.brahvim.nerd.io.asset_loader;

// To be implemented by an `enum`. For example, `CACHE_SOUNDFILES` for a `PSound` object,
// which would basically be an `AssetType<processing.sound.SoundFile>`.

public interface NerdAssetLoaderOptions {
}
