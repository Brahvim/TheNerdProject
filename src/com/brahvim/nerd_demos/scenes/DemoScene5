package com.brahvim.nerd_tests.scenes;

import com.brahvim.nerd.scene_api.NerdScene;
import com.brahvim.nerd.scene_api.SceneState;

import processing.core.PApplet;

public class DemoScene5 extends NerdScene {

    private final String TEXT = String.format(
            "Welcome to `%s`!",
            this.getClass().getSimpleName());

    @Override
    protected void setup(SceneState p_state) {
        System.out.println(this.TEXT);
        SKETCH.getCamera().pos.z = 500;
    }

    @Override
    protected void draw() {
        SKETCH.background(0x006699, PApplet.sin(super.getMillisSinceStart()));
        SKETCH.fill(233);

        SKETCH.pushMatrix();
        SKETCH.scale(2);
        SKETCH.text(this.TEXT, 0, 0);
        SKETCH.popMatrix();

        SKETCH.circle(SKETCH.mouseX - WINDOW.cx, SKETCH.mouseY - WINDOW.cy, 20);
    }

    @Override
    public void mouseClicked() {
        MANAGER.startScene(DemoScene4.class);
    }

}
